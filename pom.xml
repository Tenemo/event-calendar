<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>app</groupId>
    <artifactId>shared-calendar</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>war</packaging>

    <properties>
        <maven.compiler.release>25</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <axe.playwright.version>4.12.0</axe.playwright.version>
        <flyway.version>12.10.0</flyway.version>
        <jackson.databind.version>3.1.5</jackson.databind.version>
        <jakartaee.version>10.0.0</jakartaee.version>
        <junit.version>5.11.4</junit.version>
        <playwright.version>1.61.0</playwright.version>
        <postgresql.version>42.7.13</postgresql.version>
        <primefaces.version>15.0.16</primefaces.version>

        <liberty.maven.plugin.version>3.12.0</liberty.maven.plugin.version>
        <exec.maven.plugin.version>3.5.0</exec.maven.plugin.version>
        <maven.compiler.plugin.version>3.13.0</maven.compiler.plugin.version>
        <maven.dependency.plugin.version>3.8.1</maven.dependency.plugin.version>
        <maven.enforcer.plugin.version>3.6.3</maven.enforcer.plugin.version>
        <maven.failsafe.plugin.version>3.5.2</maven.failsafe.plugin.version>
        <maven.surefire.plugin.version>3.5.2</maven.surefire.plugin.version>
        <maven.war.plugin.version>3.4.0</maven.war.plugin.version>
        <spotbugs.maven.plugin.version>4.10.3.0</spotbugs.maven.plugin.version>
        <spotless.maven.plugin.version>3.8.0</spotless.maven.plugin.version>
        <liberty.postgresql.directory>${project.build.directory}/liberty-resources/postgresql</liberty.postgresql.directory>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>tools.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>${jackson.databind.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>jakarta.platform</groupId>
            <artifactId>jakarta.jakartaee-web-api</artifactId>
            <version>${jakartaee.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.primefaces</groupId>
            <artifactId>primefaces</artifactId>
            <version>${primefaces.version}</version>
            <classifier>jakarta</classifier>
        </dependency>
        <dependency>
            <groupId>org.primefaces</groupId>
            <artifactId>primefaces-themes</artifactId>
            <version>${primefaces.version}</version>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>${postgresql.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
            <version>${flyway.version}</version>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
            <version>${flyway.version}</version>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.microsoft.playwright</groupId>
            <artifactId>playwright</artifactId>
            <version>${playwright.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.deque.html.axe-core</groupId>
            <artifactId>playwright</artifactId>
            <version>${axe.playwright.version}</version>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>com.microsoft.playwright</groupId>
                    <artifactId>playwright</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
    </dependencies>

    <build>
        <directory>${project.basedir}/.build/package</directory>
        <finalName>shared-calendar</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven.compiler.plugin.version}</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>${maven.war.plugin.version}</version>
                <configuration>
                    <archive>
                        <addMavenDescriptor>false</addMavenDescriptor>
                    </archive>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${maven.surefire.plugin.version}</version>
                <configuration>
                    <failIfNoTests>true</failIfNoTests>
                    <useModulePath>false</useModulePath>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>${maven.enforcer.plugin.version}</version>
                <executions>
                    <execution>
                        <id>enforce-toolchain</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireJavaVersion>
                                    <version>[25,26)</version>
                                </requireJavaVersion>
                                <requireMavenVersion>
                                    <version>[3.9.11,3.9.12)</version>
                                </requireMavenVersion>
                                <dependencyConvergence/>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>${spotless.maven.plugin.version}</version>
                <configuration>
                    <formats>
                        <format>
                            <includes>
                                <include>.dockerignore</include>
                                <include>.gitignore</include>
                                <include>.mise.toml</include>
                                <include>Dockerfile</include>
                                <include>docker-compose.yml</include>
                                <include>package.json</include>
                                <include>railway.json</include>
                                <include>stylelint.config.mjs</include>
                                <include>.github/workflows/*.yml</include>
                            </includes>
                            <trimTrailingWhitespace/>
                            <endWithNewline/>
                        </format>
                    </formats>
                    <java>
                        <removeUnusedImports/>
                        <importOrder/>
                        <trimTrailingWhitespace/>
                        <endWithNewline/>
                    </java>
                </configuration>
                <executions>
                    <execution>
                        <id>check-formatting</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>com.github.spotbugs</groupId>
                <artifactId>spotbugs-maven-plugin</artifactId>
                <version>${spotbugs.maven.plugin.version}</version>
                <configuration>
                    <effort>Max</effort>
                    <threshold>Medium</threshold>
                    <includeFilterFile>${project.basedir}/config/spotbugs-include.xml</includeFilterFile>
                    <failOnError>true</failOnError>
                </configuration>
                <executions>
                    <execution>
                        <id>analyze-compiled-code</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>${maven.dependency.plugin.version}</version>
                <executions>
                    <execution>
                        <id>copy-liberty-postgresql-driver</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>org.postgresql</groupId>
                                    <artifactId>postgresql</artifactId>
                                    <version>${postgresql.version}</version>
                                    <type>jar</type>
                                    <outputDirectory>${liberty.postgresql.directory}</outputDirectory>
                                    <destFileName>postgresql.jar</destFileName>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>io.openliberty.tools</groupId>
                <artifactId>liberty-maven-plugin</artifactId>
                <version>${liberty.maven.plugin.version}</version>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>liberty-dev</id>
            <properties>
                <liberty.postgresql.directory>${project.basedir}/.liberty/user/shared/resources/postgresql</liberty.postgresql.directory>
            </properties>
            <build>
                <directory>${project.basedir}/.build/development</directory>
                <plugins>
                    <plugin>
                        <groupId>io.openliberty.tools</groupId>
                        <artifactId>liberty-maven-plugin</artifactId>
                        <configuration>
                            <changeOnDemandTestsAction>true</changeOnDemandTestsAction>
                            <runtimeInstallDirectory>${project.basedir}/.liberty/runtime</runtimeInstallDirectory>
                            <skipITs>true</skipITs>
                            <userDirectory>${project.basedir}/.liberty/user</userDirectory>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>end-to-end</id>
            <properties>
                <end.to.end.database.port>55433</end.to.end.database.port>
                <end.to.end.http.port>19082</end.to.end.http.port>
                <end.to.end.https.port>19445</end.to.end.https.port>
                <end.to.end.browser>chromium</end.to.end.browser>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <version>${exec.maven.plugin.version}</version>
                        <executions>
                            <execution>
                                <id>start-isolated-application</id>
                                <phase>pre-integration-test</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>docker</executable>
                                    <arguments>
                                        <argument>compose</argument>
                                        <argument>--project-name</argument>
                                        <argument>shared-calendar-verification</argument>
                                        <argument>--profile</argument>
                                        <argument>verification</argument>
                                        <argument>up</argument>
                                        <argument>--build</argument>
                                        <argument>--detach</argument>
                                        <argument>--wait</argument>
                                        <argument>--wait-timeout</argument>
                                        <argument>180</argument>
                                        <argument>postgres-verification</argument>
                                        <argument>web-verification</argument>
                                    </arguments>
                                    <environmentVariables>
                                        <VERIFICATION_DATABASE_PORT>${end.to.end.database.port}</VERIFICATION_DATABASE_PORT>
                                        <VERIFICATION_HTTP_PORT>${end.to.end.http.port}</VERIFICATION_HTTP_PORT>
                                        <VERIFICATION_HTTPS_PORT>${end.to.end.https.port}</VERIFICATION_HTTPS_PORT>
                                    </environmentVariables>
                                </configuration>
                            </execution>
                            <execution>
                                <id>capture-isolated-liberty-log</id>
                                <phase>post-integration-test</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>docker</executable>
                                    <arguments>
                                        <argument>exec</argument>
                                        <argument>shared-calendar-verification-web-verification-1</argument>
                                        <argument>cat</argument>
                                        <argument>/logs/messages.log</argument>
                                    </arguments>
                                    <outputFile>${project.build.directory}/end-to-end-liberty.log</outputFile>
                                </configuration>
                            </execution>
                            <execution>
                                <id>capture-isolated-application-logs</id>
                                <phase>post-integration-test</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>docker</executable>
                                    <arguments>
                                        <argument>compose</argument>
                                        <argument>--project-name</argument>
                                        <argument>shared-calendar-verification</argument>
                                        <argument>--profile</argument>
                                        <argument>verification</argument>
                                        <argument>logs</argument>
                                        <argument>--no-color</argument>
                                        <argument>web-verification</argument>
                                    </arguments>
                                    <outputFile>${project.build.directory}/end-to-end-application.log</outputFile>
                                    <environmentVariables>
                                        <VERIFICATION_DATABASE_PORT>${end.to.end.database.port}</VERIFICATION_DATABASE_PORT>
                                        <VERIFICATION_HTTP_PORT>${end.to.end.http.port}</VERIFICATION_HTTP_PORT>
                                        <VERIFICATION_HTTPS_PORT>${end.to.end.https.port}</VERIFICATION_HTTPS_PORT>
                                    </environmentVariables>
                                </configuration>
                            </execution>
                            <execution>
                                <id>stop-isolated-application</id>
                                <phase>post-integration-test</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>docker</executable>
                                    <arguments>
                                        <argument>compose</argument>
                                        <argument>--project-name</argument>
                                        <argument>shared-calendar-verification</argument>
                                        <argument>--profile</argument>
                                        <argument>verification</argument>
                                        <argument>down</argument>
                                        <argument>--volumes</argument>
                                        <argument>--remove-orphans</argument>
                                    </arguments>
                                    <environmentVariables>
                                        <VERIFICATION_DATABASE_PORT>${end.to.end.database.port}</VERIFICATION_DATABASE_PORT>
                                        <VERIFICATION_HTTP_PORT>${end.to.end.http.port}</VERIFICATION_HTTP_PORT>
                                        <VERIFICATION_HTTPS_PORT>${end.to.end.https.port}</VERIFICATION_HTTPS_PORT>
                                    </environmentVariables>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-failsafe-plugin</artifactId>
                        <version>${maven.failsafe.plugin.version}</version>
                        <configuration>
                            <failIfNoTests>true</failIfNoTests>
                            <useModulePath>false</useModulePath>
                            <environmentVariables>
                                <APP_BASE_URL>https://localhost:${end.to.end.https.port}</APP_BASE_URL>
                                <BROWSER>${end.to.end.browser}</BROWSER>
                                <END_TO_END_VERIFICATION_BASE_URL>https://localhost:${end.to.end.https.port}</END_TO_END_VERIFICATION_BASE_URL>
                                <END_TO_END_VERIFICATION_HEALTH_URL>http://localhost:${end.to.end.http.port}/health</END_TO_END_VERIFICATION_HEALTH_URL>
                                <PGDATABASE>shared_calendar_verification</PGDATABASE>
                                <PGHOST>localhost</PGHOST>
                                <PGPASSWORD>shared_calendar_verification</PGPASSWORD>
                                <PGPORT>${end.to.end.database.port}</PGPORT>
                                <PGUSER>shared_calendar_verification</PGUSER>
                            </environmentVariables>
                            <includes>
                                <include>**/*EndToEndIT.java</include>
                            </includes>
                        </configuration>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>integration-test</goal>
                                    <goal>verify</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
